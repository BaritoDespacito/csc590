import Product from "./productModel";

// TestModels was made to test the initialization syntax of products. It is not used in the final product.

const testProduct = Product(
    id = 0,
    productName = 'sampleProduct',
    type = 0,
    category = 0,
    sold = false,
);